import { WeaponDetailsCode } from './weapon-details-code';

describe('WeaponDetailsCode', () => {
  it('should create an instance', () => {
    expect(new WeaponDetailsCode()).toBeTruthy();
  });
});
